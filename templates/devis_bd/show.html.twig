{% extends 'base.html.twig' %}

{% block title %}Devis{% endblock %}

{% block body %}
    <h1>Devis</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ devi.id }}</td>
            </tr>

            <tr>
                <th>Date</th>
                <td>{{ devi.date ? devi.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_devis_b_d_index') }}">back to list</a>

    <a href="{{ path('app_devis_b_d_edit', {'id': devi.id}) }}">edit</a>

    {{ include('devis_bd/_delete_form.html.twig') }}
{% endblock %}
